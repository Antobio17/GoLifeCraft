docs/claude/
docs/sql/

# Variables de entorno con secretos (nunca versionar)
.env.local
.env.*.local
.env.prod
/backend/.env.local
/backend/.env.*.local
/backend/.env.prod

# Datos de contenedores en runtime (uploads, claves DKIM, certificados…)
/volumes/

# Utillaje de Claude Design: se queda en local, fuera del repo público.
frontend/tools/design-sync/
/.design-sync/
